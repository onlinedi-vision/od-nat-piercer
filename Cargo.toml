[package]
name = "od-nat-piercer"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
rand = "0.8"
openssl = { version = "0.10.76", features = ["vendored"] }
